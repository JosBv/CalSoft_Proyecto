# Copia este archivo a ".env" solo si necesitas apuntar a un backend
# que NO esté en el proxy de Vite (por ejemplo, en producción).
#
# En desarrollo NO hace falta: el proxy de vite.config.js redirige
# /api -> http://localhost:3000 automáticamente.

# VITE_API_URL=https://mi-backend.com/api
